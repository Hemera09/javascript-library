/**
 * 
 * @returns 
 * X: X coordinate of the center of the canvas
 * Y: Y coordinate of the center of the canvas
 */
function canvasCenter() {
    var centerX = canvas.width / 2;
    var centerY = canvas.height / 2;
    return {
    X: centerX,
    Y: centerY
    };
}